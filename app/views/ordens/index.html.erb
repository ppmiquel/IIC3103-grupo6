<p id="notice"><%= notice %></p>
<% content_for :content do %>
    <div class="main-container">
      <div class="padding-md">
        <ul class="breadcrumb">
          <li><span class="primary-font"><i class="icon-home"></i></span><a href="../index.html">Inicio</a></li>
          <li>Órdenes de compra</li>
        </ul>

        <table class="table table-striped" id="dataTable">
          <thead>
          <tr>
            <th>Idoc</th>
            <th>Canal</th>
            <th>Cliente</th>
            <th>Sku</th>
            <th>Cantidad</th>
            <th>Precio</th>
            <th>Fecha entrega</th>
            <th>Idtrx</th>
            <th>Idfact</th>
            <th>Estado</th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <% @ordens.each do |orden| %>
              <tr>
                <td><%= orden.idoc %></td>
                <td><%= orden.canal %></td>
                <td><%= orden.cliente %></td>
                <td><%= orden.sku %></td>
                <td><%= orden.cantidad %></td>
                <td><%= orden.precio %></td>
                <td><%= orden.fecha_entrega %></td>
                <td><%= orden.idtrx %></td>
                <td><%= orden.idfact %></td>
                <td><%= orden.estado %></td>
              </tr>
          <% end %>
          </tbody>
        </table>
      </div><!-- ./padding-md -->
    </div><!-- /main-container -->
<% end %>
